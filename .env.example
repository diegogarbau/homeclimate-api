# Servidor
PORT=8080

# Open-Meteo (no requiere API key)
OPEN_METEO_URL=https://api.open-meteo.com/v1

# Claude API (opcional — sin key usa el advisor mock)
CLAUDE_API_KEY=sk-ant-...

# Cache
CACHE_TTL_MINUTES=30